#!/bin/bash
# ============================================
# LD TechLab - Double-click to start (macOS)
# ============================================

cd "$(dirname "$0")"
chmod +x start.sh
./start.sh
